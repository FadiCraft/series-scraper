name: 📺 استخراج المسلسلات

on:
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 تنزيل الكود
        uses: actions/checkout@v3
        
      - name: ⚙️ إعداد Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: 📦 تثبيت الحزم (الطريقة الصحيحة)
        run: |
          echo "1. تنظيف أي ملفات قديمة..."
          rm -rf node_modules package-lock.json
          
          echo "2. تهيئة مشروع npm جديد..."
          npm init -y --silent
          
          echo "3. تثبيت axios..."
          npm install axios@1.6.0 --save
          
          echo "4. التحقق من التثبيت..."
          ls -la node_modules/
          echo "axios مثبت:" && ls node_modules/axios/ || echo "❌ فشل تثبيت axios"
          
          echo "5. عرض package.json..."
          cat package.json
          
      - name: 🚀 تشغيل البرنامج
        run: |
          echo "🔍 تشغيل fetch.js..."
          node fetch.js
          
      - name: 📊 عرض النتائج
        run: |
          echo "📁 الملفات النهائية:"
          ls -la
          
          if [ -f "test.json" ]; then
            echo "✅ test.json:"
            cat test.json
          fi
          
          if [ -f "error.json" ]; then
            echo "❌ error.json:"
            cat error.json
          fi
