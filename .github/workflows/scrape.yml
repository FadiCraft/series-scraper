name: 📺 اختبار التشغيل

on:
  workflow_dispatch:  # تشغيل يدوي فقط للاختبار

jobs:
  test-run:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 تنزيل الكود
        uses: actions/checkout@v3
        
      - name: ⚙️ إعداد Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: 🚀 تشغيل البرنامج
        run: node fetch.js
        
      - name: 📊 عرض النتيجة
        run: |
          echo "النتيجة:"
          cat test.json
